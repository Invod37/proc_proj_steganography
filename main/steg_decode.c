#include <stdio.h>
#include "steg_decode.h"

void steg_decode(const char *input_bmp, const char *msg_file) {
    printf("Running DECODE\n");
    printf("Input BMP: %s\n", input_bmp);
    printf("Output Message File: %s\n", msg_file);
    printf("Function stub execution complete.\n");
}