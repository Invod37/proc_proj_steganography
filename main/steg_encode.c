#include <stdio.h>
#include "steg_encode.h"

void steg_encode(const char *input_bmp, const char *msg_file, const char *output_bmp) {
    printf("Running ENCODE\n");
    printf("Input BMP: %s\n", input_bmp);
    printf("Message File: %s\n", msg_file);
    printf("Output BMP: %s\n", output_bmp);
    printf("Function stub execution complete.\n");
}